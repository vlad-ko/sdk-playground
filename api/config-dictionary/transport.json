[
  {
    "key": "transport",
    "displayName": "Transport",
    "description": "Custom transport function for sending events. Override default HTTP transport for custom delivery mechanisms.",
    "type": "function",
    "category": "transport",
    "required": false,
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Advanced option. Use for custom event delivery (e.g., queue systems, proxies). Most users should use \"tunnel\" instead for proxy scenarios.",
    "warnings": [
      "Advanced feature - only use if you understand transport layer",
      "Must handle retries and errors properly",
      "Consider \"tunnel\" option for simpler proxy scenarios"
    ],
    "relatedOptions": ["tunnel", "transportOptions"],
    "supportedSDKs": null
  },
  {
    "key": "tunnel",
    "displayName": "Tunnel",
    "description": "URL to proxy Sentry events through your own backend. Useful to avoid ad-blockers blocking Sentry requests.",
    "type": "string",
    "category": "transport",
    "required": false,
    "examples": [
      "/api/sentry-tunnel",
      "https://myapp.com/tunnel"
    ],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/troubleshooting/#dealing-with-ad-blockers",
    "seGuidance": "Use when ad-blockers are dropping Sentry events. Setup: create endpoint that forwards requests to Sentry. Requires backend implementation. Improves event delivery rate in browser apps.",
    "warnings": [
      "Requires backend endpoint implementation",
      "Your backend must forward events to Sentry",
      "Add proper error handling and timeouts to tunnel endpoint"
    ],
    "relatedOptions": ["dsn", "transport"],
    "supportedSDKs": null
  },
  {
    "key": "maxValueLength",
    "displayName": "Max Value Length",
    "description": "Maximum string length for event values. Longer strings are truncated.",
    "type": "number",
    "category": "transport",
    "required": false,
    "defaultValue": 250,
    "examples": ["250", "500", "1000"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Increase if you need longer stack traces or error messages. Decrease to reduce payload size. Balance between detail and quota consumption.",
    "warnings": [
      "Higher values increase payload size and quota usage",
      "Very large values may hit event size limits"
    ],
    "relatedOptions": ["beforeSend"],
    "supportedSDKs": null
  },
  {
    "key": "normalizeDepth",
    "displayName": "Normalize Depth",
    "description": "Maximum depth for normalizing objects. Controls how deep nested objects are serialized.",
    "type": "number",
    "category": "transport",
    "required": false,
    "defaultValue": 3,
    "examples": ["3", "5", "10"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Increase for deeply nested data structures. Default (3) works for most cases. Higher values increase serialization time and payload size.",
    "warnings": [
      "Higher values increase CPU usage and payload size",
      "May cause circular reference issues with very deep objects"
    ],
    "supportedSDKs": null
  },
  {
    "key": "shutdownTimeout",
    "displayName": "Shutdown Timeout",
    "description": "Time in milliseconds to wait for event delivery before shutting down. Ensures events are sent on app termination.",
    "type": "number",
    "category": "transport",
    "required": false,
    "defaultValue": 2000,
    "examples": ["2000", "5000", "1000"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Critical for serverless (Lambda, Cloud Functions). Increase if events are lost on shutdown. Default (2s) sufficient for most apps.",
    "warnings": [
      "In serverless, ensure timeout is less than function timeout",
      "Too short may lose events on shutdown",
      "Too long delays application shutdown"
    ],
    "supportedSDKs": ["javascript", "python", "go"]
  },
  {
    "key": "transportOptions",
    "displayName": "Transport Options",
    "description": "Additional options passed to the transport layer. Configure timeouts, headers, and other HTTP settings.",
    "type": "object",
    "category": "transport",
    "required": false,
    "examples": [
      "{ headers: { \"X-Custom-Header\": \"value\" } }",
      "{ fetchOptions: { keepalive: true } }"
    ],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Use for custom HTTP headers, timeouts, or fetch options. Rarely needed unless you have specific network requirements or corporate proxies.",
    "warnings": [
      "Advanced feature - most users do not need this",
      "Invalid options may prevent event delivery"
    ],
    "relatedOptions": ["transport", "tunnel"],
    "supportedSDKs": null
  },
  {
    "key": "httpProxy",
    "displayName": "HTTP Proxy",
    "description": "HTTP proxy URL for Sentry requests. Used in environments behind corporate proxies.",
    "type": "string",
    "category": "transport",
    "required": false,
    "examples": [
      "\"http://proxy.company.com:8080\"",
      "process.env.HTTP_PROXY"
    ],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Use in corporate environments with HTTP proxies. Also respects HTTP_PROXY environment variable in some SDKs. Test thoroughly as proxy issues can silently drop events.",
    "warnings": [
      "Ensure proxy allows connections to Sentry",
      "Test event delivery after configuration",
      "May need httpsProxy for HTTPS traffic"
    ],
    "relatedOptions": ["httpsProxy", "tunnel"],
    "supportedSDKs": ["python", "go", "java", "dotnet"]
  },
  {
    "key": "httpsProxy",
    "displayName": "HTTPS Proxy",
    "description": "HTTPS proxy URL for Sentry requests. Used in environments behind corporate proxies.",
    "type": "string",
    "category": "transport",
    "required": false,
    "examples": [
      "\"https://proxy.company.com:8080\"",
      "process.env.HTTPS_PROXY"
    ],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Use in corporate environments with HTTPS proxies. Also respects HTTPS_PROXY environment variable. Test thoroughly as proxy issues can silently drop events.",
    "warnings": [
      "Ensure proxy allows connections to Sentry",
      "May need to configure SSL certificates"
    ],
    "relatedOptions": ["httpProxy", "tunnel"],
    "supportedSDKs": ["python", "go", "java", "dotnet"]
  },
  {
    "key": "maxQueueSize",
    "displayName": "Max Queue Size",
    "description": "Maximum number of events to queue for sending. Events beyond this limit are dropped.",
    "type": "number",
    "category": "transport",
    "required": false,
    "defaultValue": 30,
    "examples": ["30", "50", "100"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Increase if experiencing event drops during bursts. Decrease to limit memory usage. Default (30) works for most cases. Monitor client reports for dropped events.",
    "warnings": [
      "Higher values use more memory",
      "Events beyond limit are dropped silently",
      "Consider if you are hitting limits - may indicate a bigger issue"
    ],
    "relatedOptions": ["shutdownTimeout"],
    "supportedSDKs": null
  },
  {
    "key": "caCerts",
    "displayName": "CA Certificates",
    "description": "Path to custom CA certificates for SSL verification. Used with self-signed certificates or corporate CAs.",
    "type": "string",
    "category": "transport",
    "required": false,
    "examples": [
      "\"/path/to/ca-bundle.crt\"",
      "process.env.SSL_CERT_FILE"
    ],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Required for self-hosted Sentry with custom SSL certs. Also needed in corporate environments with SSL inspection. Verify SSL configuration after setup.",
    "warnings": [
      "Invalid certificates will cause connection failures",
      "Test thoroughly after configuration"
    ],
    "relatedOptions": ["dsn", "httpProxy"],
    "supportedSDKs": ["python", "go"]
  }
]
