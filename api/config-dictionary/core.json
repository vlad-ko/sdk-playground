[
  {
    "key": "dsn",
    "displayName": "DSN",
    "description": "The Data Source Name (DSN) tells the SDK where to send events. This is the only required configuration option.",
    "type": "string",
    "category": "core",
    "required": true,
    "examples": [
      "https://examplePublicKey@o0.ingest.sentry.io/0",
      "https://abc123def456@o123456.ingest.us.sentry.io/7890"
    ],
    "docsUrl": "https://docs.sentry.io/product/sentry-basics/dsn-explainer/",
    "seGuidance": "The DSN is unique per project and contains the project ID. Always use HTTPS DSNs. For self-hosted, the domain will be your Sentry instance URL. Keep DSNs secure - they allow writing to your project.",
    "warnings": [
      "Never commit DSNs to public repositories",
      "Use environment variables to store DSNs",
      "Each project has a unique DSN - using the wrong DSN sends data to the wrong project"
    ],
    "relatedOptions": ["tunnel", "transport"],
    "supportedSDKs": null
  },
  {
    "key": "environment",
    "displayName": "Environment",
    "description": "The environment name, such as \"production\" or \"staging\". This helps separate events from different deployment environments.",
    "type": "string",
    "category": "core",
    "required": false,
    "defaultValue": "production",
    "examples": ["production", "staging", "development", "qa", "test"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/environments/",
    "seGuidance": "Always set environment to match your deployment stage. This allows filtering in Sentry UI and helps with release tracking. Common practice: use NODE_ENV or similar environment variable.",
    "warnings": [
      "Default is \"production\" - make sure to set this for non-production environments",
      "Case-sensitive - be consistent with naming"
    ],
    "relatedOptions": ["release"],
    "supportedSDKs": null
  },
  {
    "key": "release",
    "displayName": "Release",
    "description": "The release version of your application. Used to associate errors with specific releases and enable features like suspect commits.",
    "type": "string",
    "category": "core",
    "required": false,
    "examples": [
      "my-app@1.0.0",
      "my-app@2.3.1+build.123",
      "abc123def456",
      "2024-01-15.1"
    ],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/releases/",
    "seGuidance": "Use semantic versioning (semver) or commit SHA. Must match the release created via Sentry CLI or API for full release features (suspect commits, deploy tracking). Format: name@version recommended.",
    "warnings": [
      "Must match exactly with releases created in Sentry for suspect commits to work",
      "Case-sensitive"
    ],
    "relatedOptions": ["dist", "environment"],
    "supportedSDKs": null
  },
  {
    "key": "dist",
    "displayName": "Distribution",
    "description": "The distribution identifier. Used to distinguish between multiple builds of the same release (e.g., different app store versions).",
    "type": "string",
    "category": "core",
    "required": false,
    "examples": ["123", "alpha", "v1.0.0-build.456"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/releases/",
    "seGuidance": "Use for mobile apps with multiple builds per release or when deploying to different distribution channels. Common for app stores (iOS, Android) where build number differs from version.",
    "relatedOptions": ["release"],
    "supportedSDKs": null
  },
  {
    "key": "debug",
    "displayName": "Debug Mode",
    "description": "Enable debug mode to print SDK diagnostic information to the console. Useful for troubleshooting SDK integration issues.",
    "type": "boolean",
    "category": "debug",
    "required": false,
    "defaultValue": false,
    "examples": ["true", "false"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Enable during initial integration to verify SDK is working correctly. Always disable in production - creates verbose console output.",
    "warnings": [
      "Never enable in production - exposes internal SDK operations",
      "May impact performance with high event volumes"
    ],
    "relatedOptions": ["logLevel"],
    "supportedSDKs": null
  },
  {
    "key": "maxBreadcrumbs",
    "displayName": "Max Breadcrumbs",
    "description": "Maximum number of breadcrumbs to capture. Breadcrumbs are trail of events leading up to an error.",
    "type": "number",
    "category": "core",
    "required": false,
    "defaultValue": 100,
    "examples": ["50", "100", "200"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/enriching-events/breadcrumbs/",
    "seGuidance": "Default (100) works for most cases. Increase for complex applications with many user interactions. Decrease to reduce payload size if hitting quota limits quickly.",
    "warnings": [
      "Higher values increase event payload size",
      "May impact quota consumption"
    ],
    "relatedOptions": ["beforeBreadcrumb"],
    "supportedSDKs": null
  },
  {
    "key": "enabled",
    "displayName": "Enabled",
    "description": "Whether the SDK should capture and send events. Set to false to disable the SDK entirely.",
    "type": "boolean",
    "category": "core",
    "required": false,
    "defaultValue": true,
    "examples": ["true", "false"],
    "docsUrl": "https://docs.sentry.io/platform-redirect/?next=/configuration/options/",
    "seGuidance": "Use to conditionally disable Sentry (e.g., in local development). Better than not calling Sentry.init() because SDK API remains available (calls become no-ops).",
    "relatedOptions": ["dsn"],
    "supportedSDKs": null
  }
]
